# 'demo' runs against a clearly-marked in-app sample pool; 'live' calls the Mzobs employer API.
VITE_TALENT_SOURCE=demo
VITE_API_URL=http://localhost:4000/api/employer
VITE_FILE_BASE_URL=http://localhost:4000
VITE_LANDING_URL=http://localhost:5176
# Dev proxy target for /api and /files (existing backend, unchanged)
BACKEND_URL=http://localhost:4000
